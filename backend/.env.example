# Flask app secret key (used for session signing)
SECRET_KEY=your-secret-key-here

# JWT secret key (used for token signing — MUST be different from SECRET_KEY in production)
JWT_SECRET_KEY=your-jwt-secret-key-here

# Database connection URI (SQLite for dev, PostgreSQL for production)
DATABASE_URL=sqlite:///dev.db

# Upload directory (relative to backend/, or absolute path)
UPLOAD_FOLDER=uploads

# Maximum upload file size in megabytes
MAX_FILE_SIZE_MB=16
